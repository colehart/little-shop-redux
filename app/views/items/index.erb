<h1>Items</h1>
<a href="/items/new" class="button">Create A New Item</a>

<% @items.each do |item| %>
<div class='item'>
  <img src="<%= item.image %>" alt="">
  <h3><a href="/items/<%= item.id %>"><%= item.name %></a></h3>
  <p>$<%= item.unit_price/100 %></p>
</div>
<% end %>
